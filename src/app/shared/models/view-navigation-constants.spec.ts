import { ViewNavigationConstants } from './view-navigation-constants';

describe('ViewNavigationConstants', () => {
  it('should create an instance', () => {
    expect(new ViewNavigationConstants()).toBeTruthy();
  });
});
